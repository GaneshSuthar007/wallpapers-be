export enum Genders {
  male = 'Male',
  female = 'Female',
  others = 'Others',
}

export enum UserType {
  admin = 'admin',
  manager = 'manager',
  volunteer = 'volunteer',
}

export enum VerificationStatus {
  verified = 'verified',
  pending = 'pending',
  rejected = 'rejected',
}

export enum ProfessionType {
  student = 'student',
  working = 'working',
  ngo = 'NGO',
  government = 'Government',
  school_coaching = 'school/coaching',
}
